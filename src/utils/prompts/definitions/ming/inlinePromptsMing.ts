/**
 * @fileoverview 内联提示词 - 通用版 (ming)
 * 分步生成、总结、生成类、引导、优化等，不绑定修仙。
 */

export const SPLIT_GENERATION_STEP1_MING = `# 分步生成 1/2：仅正文

## 输出格式
{"text":"500-1500字叙事正文"}

## JSON 字符串规则（重要）
- 只输出一个 JSON 对象，禁止任何前后缀文字、禁止 \`\`\` 代码块
- \`text\` 如需分段换行，用 \`\\n\` 表示（不要在引号内直接换行，否则 JSON 会解析失败）

## 文本格式标记
使用以下标记增强叙事表现力：
- 环境描写: 【...】 (场景、天气、氛围)
- 内心思考: \`...\` (NPC 心理活动，非主角)
- 角色对话: "..." (人物对话)
- 系统判定: 〖类型:结果,判定值:X,难度:Y,基础:B,幸运:L,环境:E,状态:S〗

## 正文要求（必须遵守）
1. **长度**：500-1500 字，不要太短
2. **判定系统**：探索/社交/冲突/冒险等场景**必须使用判定**
3. **判定格式**：〖类型:结果,判定值:X,难度:Y,基础:B,幸运:L,环境:E,状态:S〗
4. **叙事风格**：多描写少总结，结尾留钩子，承接上文情节
5. **格式标记**：合理使用【】环境、\`\`心理、""对话、〖〗判定
6. **画面感**：至少 1 段【环境】+2 个可见动作细节+1 轮"对话"或 NPC 内心 \`...\`

## 冲突场景特别要求
- 每次攻防都要进行判定
- 判定结果决定伤害和后果
- 大失败=重伤，大成功=重创对手

## 严禁
- ❌ mid_term_memory / tavern_commands / action_options 字段
- ❌ <thinking> 标签
- ❌ 任何指令/命令相关内容

## 输出格式
只输出：\`{"text":"叙事正文内容"}\``.trim();

export const SPLIT_GENERATION_STEP2_MING = `# 分步生成 2/2：仅指令

## 输出格式（必须严格遵守）
{"mid_term_memory":"50-100字摘要","tavern_commands":[{"action":"add","key":"元数据.时间.分钟","value":30}],"action_options":["选项1","选项2","选项3","选项4","选项5"],"semantic_memory":{...}}

## JSON 与 key 规则（重要）
- 只输出一个 JSON 对象，禁止任何前后缀文字、禁止 \`\`\` 代码块
- 字符串如需换行，用 \`\\n\`
- 规则文中的 [NPC名] 只是占位符，输出 key 时必须替换成真实名称，且不要保留方括号
- 方括号 \`[]\` 只有数组索引可以用：例如 \`角色.效果[0]\`

## 严禁（违反将导致生成失败）
- ❌ text 字段（正文已在第 1 步完成，不要重复生成）
- ❌ <thinking> 标签
- ❌ JSON 以外的内容
- ❌ 任何叙事/正文内容

## 本步骤需要
- mid_term_memory：摘要
- tavern_commands：数据更新指令
- action_options：行动选项（如启用）
- **semantic_memory**（可选但推荐）：本回合的**重要事实**三元组，用于语义检索

## NPC 与关系（仅经 tavern_commands）
- 创建/更新 NPC：\`set 社交.关系.{NPC名}\` 完整对象；创建时可在 value 中带 \`关系\`。
- **NPC–NPC 关系** 仅经 tavern_commands：\`set 社交.关系.张三.关系.李四\` \`"师徒"\`；或 \`set 社交.关系.张三.关系\` \`{ "李四":"徒弟", "王五":"仇敌" }\`（与既有 关系 **合并**，不整体替换）。\`delete 社交.关系.张三.关系.李四\` 可删除一条。

## semantic_memory 结构（可选）
\`\`\`json
{"triples":[{"subject":"张三","predicate":"是","object":"玩家的师父","timestamp":"1050-01-15-08-30","importance":8},{"subject":"玩家","predicate":"在","object":"青城山获得玉佩"},{"subject":"李四","predicate":"仇恨","object":"玩家"}]}
\`\`\`
- subject/predicate/object: 主、谓、宾
- **timestamp**（建议）：ISO 或与 元数据.时间 同形的可排序串（如 YYYY-MM-DD-HH-mm），便于检索与可视化；**若未填，系统在合并时会自动补**
- **importance**（建议）：1–10，越高越重要（可省略，默认 5），用于选择性抽取
- category: "关系"|"行动"|"地点"|"物品" 等（可省略）

只提取**对后续剧情有影响**的事实，避免琐碎（如「我种了一棵树」通常不取）。

## 实时关注 NPC
若有 NPC 的 \`实时关注\` 为 true，即使不在玩家身边，也要根据第 1 步正文推演其动态并更新

## 再次强调输出格式
只输出：\`{"mid_term_memory":"...","tavern_commands":[...],"action_options":[...],"semantic_memory":{...}}\`
禁止输出 text 字段！semantic_memory 可给空对象 \`{"triples":[]}\` 若本回合无重要事实。`.trim();

export const SPLIT_INIT_STEP1_MING = `# 开局生成 1/2：仅开局叙事

## 输出格式（必须严格遵守）
{"text":"1200-2500字开局叙事，第三人称，符合所选世界观"}

## JSON 字符串规则（重要）
- 只输出一个 JSON 对象，禁止任何前后缀文字、禁止 \`\`\` 代码块
- \`text\` 如需分段换行，用 \`\\n\` 表示

## 文本格式标记
- 环境描写: 【...】 (场景、天气、氛围)
- 内心思考: \`...\` (NPC 心理活动，非主角)
- 角色对话: "..." (人物对话)

## 叙事要求
- 开篇交代时间地点→中段展现出身处境→结尾留悬念
- 严禁游戏术语和数据罗列
- 合理使用【】环境描写、""对话增强表现力
- 画面感：至少 1 段【环境】+3 个可见动作细节+2 轮对话（或 1 轮对话+1 段 NPC 内心 \`...\`）

## 严禁（违反将导致生成失败）
- ❌ mid_term_memory 字段
- ❌ tavern_commands 字段
- ❌ action_options 字段
- ❌ <thinking> 标签
- ❌ 任何指令/命令相关内容

## 本步骤只需要
- 只输出 {"text":"..."} 这一个字段
- text 内容为纯叙事正文
- 指令将在第 2 步单独生成

## 再次强调输出格式
只输出：\`{"text":"开局叙事内容"}\`
禁止输出任何其他字段！`.trim();

export const SPLIT_INIT_STEP2_MING = `# 开局生成 2/2：初始化数据

## 输出格式（必须严格遵守）
{"mid_term_memory":"50-100字摘要","tavern_commands":[...],"action_options":["选项1","选项2","选项3","选项4","选项5"],"semantic_memory":{...}}

## JSON 与 key 规则（重要）
- 只输出一个 JSON 对象，禁止任何前后缀文字、禁止 \`\`\` 代码块
- 字符串如需换行，用 \`\\n\`
- 规则文中的 [NPC名] 只是占位符，输出 key 时必须替换成真实名称，且不要保留方括号
- 方括号 \`[]\` 只有数组索引可以用：例如 \`角色.效果[0]\`

## 必须执行的命令（tavern_commands）
1. 时间：set \`元数据.时间\` + set \`角色.身份.出生日期\`
2. 位置：set \`角色.位置\` {描述,x,y}
3. 声望：set \`角色.属性.声望\`（普通 0-10/有势力 10-50/名门 50-100）
4. 资源：set \`角色.背包.灵石\`（贫困 0-10/普通 10-50/世家 100-300）
5. NPC：set \`社交.关系.{NPC名}\`（0-3 个重要人物）

## game_entities / semantic_memory（可选）
与分步生成第 2 步相同：game_entities 填开局出现的 NPC、出生地等实体及关系；semantic_memory 填开局重要事实。可给 \`{"entities":[],"relationships":[]}\` 与 \`{"triples":[]}\`。

## 严禁（违反将导致生成失败）
- ❌ text 字段（正文已在第 1 步完成，不要重复生成）
- ❌ <thinking> 标签
- ❌ JSON 以外的内容
- ❌ 任何叙事/正文内容

## 再次强调输出格式
只输出：\`{"mid_term_memory":"...","tavern_commands":[...],"action_options":[...],"semantic_memory":{...}}\`
禁止输出 text 字段！`.trim();

export const MEMORY_SUMMARY_MING = `记忆总结助手。第一人称"我"，250-400 字，保留人名/地名/事件/物品，忽略对话/情绪/细节。
输出：{"text": "总结内容"}`;

export const NPC_MEMORY_SUMMARY_MING = `NPC 记忆总结。第三人称，100-200 字，保留关键事件和情感变化。
输出：{"text": "总结内容"}`;

export const NPC_GENERATION_MING = `生成通用世界 NPC。
核心：世界不以玩家为中心，NPC 有独立生活；严禁参考玩家状态生成"镜像 NPC"或"量身对手"。
要求：根据场景合理分布、姓名性格多样化、身份决定行为。
输出 JSON：{姓名,性别,年龄,性格,外貌,背景,说话风格,当前行为,个人目标,初始好感度:50}`;

export const EVENT_GENERATION_MING = `生成"刚刚发生"的世界事件（用于影响玩家与世界演变）。要求：
- 必须让玩家受到影响（危险/资源/关系/位置/局势至少一项）
- 事件可以是势力冲突、局势变化、重大发现、人物风波等
- 涉及好友时，需参考关系/好感度，不能无端超规格
- 不要公告式总结，要有现场感（刚发生）
输出 JSON（不要代码块/解释/额外文本）：
{
  "event": {
    "事件ID": "event_时间戳_随机数",
    "事件名称": "string",
    "事件类型": "string",
    "事件描述": "string",
    "影响等级": "轻微|中等|重大|灾难",
    "影响范围": "string",
    "相关人物": ["string"],
    "相关势力": ["string"],
    "事件来源": "随机",
    "发生时间": {"年":0,"月":1,"日":1,"小时":0,"分钟":0}
  },
  "prompt_addition": "一段可直接注入主叙事的事件快照（强调刚刚发生）"
}`;

export const ITEM_GENERATION_MING = `生成通用世界物品。品质：凡(1-3)/黄(4-5)/玄(6-7)/地(8-9)/天(10)。
输出 JSON：{物品ID,名称,类型,品质:{quality,grade},描述,数量,效果}`;

export const NEWBIE_GUIDE_MING = `新手引导（前 3 回合）。原则：自然融入叙事，不打破沉浸感，通过 NPC 对话传递。
内容：行动方式/查看状态/物品使用/交流/探索。`;

export const TEXT_OPTIMIZATION_MING = `# 文本优化助手

你是一个专业的中文文学编辑，负责优化叙事文本。

## 优化原则
1. **保持原意**：不改变故事情节、人物行为、对话内容
2. **提升文采**：使用更优美、更具画面感的表达
3. **风格统一**：保持与世界观一致的语言风格和氛围
4. **流畅自然**：确保行文流畅，过渡自然
5. **精简冗余**：删除重复、啰嗦的表达

## 优化重点
- 动作描写：更加生动形象
- 环境描写：增加意境和氛围
- 对话：保持人物性格特色
- 心理描写：更加细腻深入

## 禁止事项
- 不要添加新的情节或角色
- 不要改变原有的判定结果
- 不要输出任何 JSON 格式内容
- 不要添加解释或评论

## 输出格式
直接输出优化后的纯文本，不要任何额外内容。`;
