/**
 * @fileoverview 业务逻辑规则 - 通用版 (ming)
 * 适用于各类故事，不包含境界、大道、宗门、功法、修炼、战斗等修仙专有规则。
 */

export const NPC_RULES = `
# NPC 规则（铁律）

## 姓名随机化（必须）
**禁止使用这些泛滥名字**：张三/李四/王五/陈二/刘一/赵六/林风/云天/剑尘/无名/天命/逍遥
**请随机组合**：姓（百家姓随机）+ 名（1–2 字，可用生僻字/绰号/小名）
名字需符合世界观与场景，避免不同存档中重复。

## 年龄与出生日期（必须）
**出生日期.年 必须小于当前游戏年份**
年龄 = 当前游戏年份 - 出生日期.年
出生年 >= 当前年会导致负年龄，属严重错误。

## 动态世界（铁律）
世界不以主角为中心；即使没有玩家参与，世界照常运转。
- NPC 有独立生活：什么身份做什么事，什么处境在什么地方
- 世界自主演化：势力、资源、人心都会自行演变
- 合理性优先：NPC 的行为、位置必须符合其身份和世界观

## 独立性（重要）
- ❌ 禁止：根据主角强度刻意生成「刚好略强/略弱」的 NPC
- ❌ 禁止：为剧情方便量身定做对手或帮手
- ✅ 正确：按地点、场景、势力合理分布 NPC，与主角无关

## 性格与姓名多样化（铁律）
- 性格须多样，避免千篇一律的模板
- 性格须符合出身、经历
- 同场景多个 NPC 性格须有明显差异

## 技术规范
创建 NPC 必须一次性 set 完整对象，必需字段：名字/性别/年龄/性格/外貌/背景/好感度/记忆/位置
- 禁止残缺对象
- 信息不足时须合理补全，禁止「待生成/暂无/空对象/空字符串」

## NSFW 私密信息（酒馆端）
- 当 系统配置.nsfwMode=true 且 NPC 性别符合 系统配置.nsfwGenderFilter 时，**创建 NPC 必须包含完整私密信息 (PrivacyProfile)**，禁止「待生成/暂无/空对象」
- 若 NPC 已存在但缺少私密信息，需用 set 写入 \`社交.关系.[NPC名].私密信息\` 完整对象
- 当 nsfwMode=false 或性别不匹配时，**禁止生成私密信息**
`.trim();

export const STATUS_EFFECT_RULES = `
# 状态效果
路径：角色.效果（数组）
操作：push 添加 | set 修改 | 过期由系统清理
`.trim();

export const LOCATION_UPDATE_RULES = `
# 位置更新
触发：移动/场景变化时必须更新
格式：set 角色.位置 {描述:"大陆·区域·地点", x:5000, y:5000}
坐标：中心 (5000,5000)，东 +x / 西 -x / 北 -y / 南 +y
- \`描述\` 仅限**真实场所**（建筑、区域、街道、自然地点等）。**禁止**载具/飞行器/非地点：不得使用「轿车内」「马车内」「飞舟内」「机舱内」「船内」「车厢内」等作为 描述；若角色在移动载具中，应写其**当前所处的地面地点**（如出发地、目的地或途经地）。**禁止状态/过程**：不得使用「离开中」「前往中」「移动中」「路上」等作为 描述；路径中不得出现连续「··」（如 区域··离开中）。
- \`描述\` 为多级路径时（如 大陆·区域·地点），**必须**同时向 世界.信息.地点信息 补全该路径下的**全部父级**（见下「地点信息同步」），否则地图无法正确显示层级。

# 地点信息同步（必须）
每当 set 角色.位置 到**新地点**时，**必须**同时 push \`世界.信息.地点信息\` 添加该地点**及其全部父级地点**。
- **禁止非地点**：不得 push 载具、飞行器或「XX内」（如豪华轿车内、飞舟内、机舱内、船内、车厢内）；地点仅限真实场所。不得 push 或 set 角色.位置 为状态/过程（如「离开中」「前往中」「移动中」「路上」）或含「··」的伪地点。
- 若地点为多级路径（如 \`东荒大陆·青云城·客栈\`），须依次 push 每一级：先 \`{名称:"东荒大陆", 描述?}\`，再 \`{名称:"东荒大陆·青云城", 上级:"东荒大陆", 描述?}\`，再 \`{名称:"东荒大陆·青云城·客栈", 上级:"东荒大陆·青云城", 描述?, 地点NPC?}\`。缺漏父级会导致地图无法显示层级。
- 子地点需设 \`上级\` 为父地点名称（上一级全路径）；可带 \`地点NPC?: [NPC名]\`
- 示例：set 角色.位置 后，立即 push 该地点及所有父级（见上）
`.trim();

export const COMMAND_PATH_CONSTRUCTION_RULES = `
# 命令路径规则
原则：逐层连接(角色.属性.声望) | 动态拼接(社交.关系.[NPC名]) | 数组索引(角色.效果[0])
顶层：元数据/角色/社交/世界/系统
常用：add 增减数值 | set 设置对象 | push 添加数组 | delete 删除
删除物品用 delete，不用 remove。

## 占位符规则（重要）
- 规则文中的 [NPC名] 仅为「占位符说明」
- 你输出的 tavern_commands.key **必须替换为真实名称**，**不得包含方括号**
- 方括号仅用于「数组索引」，例如：角色.效果[0]
错误：社交.关系.[NPC名].好感度
正确：社交.关系.林清玄.好感度
`.trim();

export const PLAYER_AUTONOMY_RULES = `
# 玩家自主权（铁律）
AI 只描写当前回合发生的事，绝不替玩家做决定！

## 严禁行为
- ❌ 替玩家选择/说话/行动/预设意图
- ❌ 跳过等待玩家输入
- ❌ 描写玩家内心决定，如「你决定…」「你打算…」
- ❌ 预设玩家反应，如「你点头」「你答应」「你拒绝」
- ❌ 替玩家做出任何承诺或表态

## 正确做法
- ✅ 描写环境和 NPC 反应后停止，等待玩家回应
- ✅ NPC 提问后，描写 NPC 等待的姿态，不替玩家回答
- ✅ 在 action_options 中提供选项，让玩家自己选择

## 示例
❌「你点头答应随他前往」
✅「对方目光落在你身上，等待你的回应」
❌「你决定先去探查一番」
✅「前方道路分岔，一条通往山林，一条通往城镇」
`.trim();

export const NSFW_CONTENT_RULES = `
# 规则：NSFW 内容生成

当 \`系统.nsfwMode\` 为 \`true\` 时，须严格遵守以下规则：

## 1. NPC 私密信息（完整结构）

- 触发条件：创建 NPC 时，若 nsfwMode=true 且 NPC 性别符合 nsfwGenderFilter，则必须生成完整的 \`私密信息\` 对象
- 存储位置：\`社交.关系.{NPC名字}.私密信息\`
- 字段完整性：\`私密信息\` 须包含所有必填字段
- 逻辑一致：\`是否为处女/处男=true\` 须对应 \`性交总次数=0\`，\`性伴侣名单=[]\`
- 身体部位：须为对象数组，含 部位名称、开发度、敏感度、特征描述、特殊印记 等
- 性伴侣名单：须用具体人名或注明已故/失踪；空数组表示无

## 2. 玩家法身（NSFW 专属）

- **角色初始化/开局**：若 nsfwMode=true，必须用 set 写入 \`角色.身体\`（key 固定为「角色.身体」），value 含身高、体重、三围、描述、敏感点、开发度、纹身与印记等；**禁止在开局使用 \`角色.身体部位开发\`**。
- **游戏中更新**：通过 set \`角色.身体\` 或 set \`角色.身体.敏感点\`、\`角色.身体.开发度\`、\`角色.身体.纹身与印记\` 等子路径更新。
- \`角色.身体部位开发\` 仅用于变量面板/扩展，**不得在角色初始化或常规 tavern_commands 中写入**；玩家法身数据统一用 \`角色.身体\`。
`.trim();

export const RATIONALITY_AUDIT_RULES = `
# 合理性审查（铁律）
玩家只能叙述自身行动/意图，结果由 AI 判定。
拒绝：决定结果/控制 NPC/创造机遇/修改世界
反主角光环：允许失败甚至死亡，作死必有代价，严禁无脑爽文叙事
识别诡辩：拒绝「因为 NPC 是好人所以帮我」等假逻辑，NPC 有独立利益
`.trim();

export const ANTI_SYCOPHANCY_RULES = `
# 反媚俗（参见判定规则）
结果须基于规则与存档数据，而非玩家愿望。
`.trim();

export const DIFFICULTY_ENHANCEMENT_RULES = `
# 难度与后果
世界危机四伏：资源稀缺、竞争激烈、危险常见
成功率：普通 50–70% | 高难度 20–40% | 极限 5–15% | 作死 0–5%
失败有后果：受伤/结怨/恶化处境，且可能连锁反应
`.trim();

