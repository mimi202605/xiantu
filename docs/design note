人物意志系统：
主要来说是为了让NPC人物有自主意识，有自主行为，有自主决策，有自主选择，有自主行动，有自主结果。增加代入感。
1. 每个NPC需要根据剧情和人物设定保持一个愿望清单wishlist
2. 待办事件清单由长中短期的待办事件组成，不唯一可有多个，而待办事件则有生成时间，强制性，主观意愿，对事件看法，事件结果（如果结束事件生成结果）。
3. 每个待办事件都为game entity保存在triple中和NPC关联，也需要被保存在人物关系人物的json之中。
4. 需要有元数据
4. 将以上信息显示在人物关系之中。
5. 需要标记NPC的行为是否被世界呼吸系统回应（世界系统会周期性的更新NPC决策与行为的结果并对NPC形成影响）。

新闻系统

故事大纲编写系统，类似于conditional的prompt注入，用于编写接下来章节的大纲，然后通过ai整理成分段式的故事情节

prompt文档归类，类似workflow或者Claude code那种instructions
1. 提示词管理升级

给人物生成头像，不同状态下的头像（认真，发情，贤者状态）

主线故事框架，NPC来访/主动交互，互动触发，NPC互动按钮

世界心跳系统：
1. 周期性更新NPC人物信息（位置、在做事项、状态、内心想法等），应该考虑世界大小、NPC活动范围、NPC活动频率、NPC活动时间等因素。
2. 系统会周期性的更新NPC决策与行为的结果并对NPC形成影响
3. 为了不占用大量的token，更新应该是周期性的与有选择性的，应该判断NPC与目前事件和人物的关联性实行更新。
4. 世界事件发生时因立马更新所有npc的人物信息，更新主要与世界事件强关联和主角与其他关联的npc（检索人物关系graph）弱关联。
5. 在世界信息下增加一个ui部件--世界呼吸/世界心跳。用于显示每次人物信息的更新（更新内容和更新时间）方便玩家查看。
6. 系统可在设置中开关
7. 结合地图系统可以分步更新人物去向。


提示词管理系统 ✅️

地图系统：✅️
地图地点根据剧情逐步生成，我们要做的仅仅是记录与显示。形成一种rts中的迷雾系统一种的感觉，根据玩家探索程度逐步揭露地图并记录。
地点的进入与细分。譬如说去了百货大楼->我们生成百货大楼地点和描述，可在UI查看->进入百货大楼->生成内部一楼/多楼地点和描述，可在UI查看->进入某商店->生成商店内部与描述
为了不让模型丢失注意力，结构应该尽量简洁，但可被后续查阅（关键字等）。可考虑异步生成
各个地方需要有各个地方的NPC（重要与普通NPC）
玩家可以通过地图回访查看这些地方，或者查看这些地方的NPC。UI的访问必须是层层打开的。例如为点开百货大楼里面有层数，每个层数里又有商品等...
关于UI的implementation我们可以参考之前的commit，我们删掉了原有的地图系统，但是此次我们在功能与设计上完全不一样，因此新的架构是prefer的。
在玩家到达新地点时生成路人npc
新：
***地图rescale功能： ✅️
1. 当一个地点被放大时，应该重新设置外部地点和其内部地点图标的比例（外面的框要足够大，可以占满屏幕，然后内部图标可以小但要尽可能的展开，不要挤在一起）。现在比例保持恒定，因此当放大时多内部地点或者有多层嵌套时内部地点结构显得很拥挤
2. 内部结构如果还有内部结构，第1点的内容同样适用。即可以双击放大然后遵从第一点的规则。

***每次更新地点信息的地点npc时需要检查当前已保存数据中所有的地点npc，移除其他地点的该npc。

**地图NPC生成按键/系统 
可开启的地图人物预生成
增加地图功能按键生成NPC，即可在该地点生成普通NPC（和极少量的重要NPC），功能可被认为是观察该地点身边的人。


npc人物系统：

重点NPC与普通NPC ✅️
与玩家交集较多的为重点NPC，而玩家没有交集的只是被系统被动生成的背景NPC为普通NPC。
与地图系统的关联：一个地点中系统会随机生成普通NPC（多）和重要NPC（少）
与核心故事系统的关联：故事中出现并提及的是重要NPC
普通NPC可以通过玩家与其交流变为重要NPC

***禁止普通npc进入玩家的关系网络

重要NPC设计
一、多维人设体系：从「标签化」到「有矛盾的立体人格」
核心定位：构建立体人格的底层定义，打破单一标签限制
细化落地：
配置 3-5 个显性特质 + 1-2 个隐性特质，为每个特质设定初始权重（如显性「温柔 60%」、隐性「敏感 20%」）
设计 1 组核心矛盾（如「复仇欲 vs 仁善」），明确矛盾优先级（危机场景优先某一动机）
设定关键事件触发成长（如目睹同门被害），触发后自动调整特质权重（如「狠厉」从 10% 提升至 60%）
跨模块交互：
核心矛盾触发的事件自动录入情感记忆日志
成长阈值触发后，同步更新成长与变化机制中的生命周期目标
特质权重决定社交流言系统的流言传播风格（如「聪慧」特质 NPC 传播流言会补充细节）

二、社交流言系统
核心定位：通过 NPC 社交生成反馈，同时制造剧情钩子
细化落地：
NPC 互动时随机生成三类流言：真实信息（影响玩家声誉）、隐藏信息（触发剧情）、虚假信息（制造误解）
为虚假流言设计澄清机制（如玩家提供证据反驳）
流言传播范围与 NPC 社交关系强度挂钩（亲密关系传播更快）
跨模块交互：
流言内容关联 NPC 社交网络，形成连锁传播
虚假流言自动写入情感记忆日志，影响 NPC 对玩家的态度
流言中的信息痕迹可转化为 NPC 世界互动痕迹（如他人提及的「某 NPC 行踪」成为信息痕迹）

三、情感记忆日志
核心定位：作为 NPC 情感行为的核心数据源，保障情感反馈连贯性
细化落地：
录入情绪值波动 ±10 以上的事件，记录事件类型、强度、关联对象
区分核心记忆（永久保存，如濒死被救）与普通记忆（30 天衰减，如日常争执）
同一类型事件累计 3 次触发情感质变（如 3 次相助→「无条件信任」，3 次背叛→「信任崩塌」）
跨模块交互：
记忆内容直接影响多维人设体系的特质权重调整
NPC 向第三方提及记忆内容时，联动社交流言系统生成流言
相似场景触发回忆时，可激活 NPC 专属小任务（如玩家再提「帮忙」时，触发关联记忆的任务）

四、NPC 世界互动痕迹
核心定位：为 NPC 自主行为提供可视化实证，强化真实感
细化落地：
物理痕迹：NPC 完成自主行为后留下具象证据（炼丹→药渣、探亲→书信）
信息痕迹：其他 NPC 闲聊时提及某 NPC 的行为或状态（如「苏清瑶今早去了后山」）
状态痕迹：NPC 自主行为后产生的持续性表现（修炼→属性小幅提升、受伤→带伤行动）
跨模块交互：
物理痕迹与信息痕迹可被玩家触发，生成社交流言系统中的流言
状态痕迹影响成长与变化机制的生命周期阶段判定（如受伤状态延长青年期成长周期）
物理痕迹可作为 NPC 专属小任务的触发条件（如发现书信→触发「转交信件」任务）

五、npc 对话系统
与主故事异步进行（即独立于主故事系统，但是仍小范围更新数据），
UI的话可以在NPC页面与其对话，通过llm处理。
此类对话仅改变此NPC的信息（例如，改变NPC的动机，或者对某事/人的看法。然后此NPC通过世界呼吸系统更新的行为可能会改变等...）
此类对话需要更新NPC的记忆让NPC在主线故事中能记得对话。

所有的NPC设计在交互上都应该与已有的功能相仿，例如游戏变量中的数据可访问性与可改写性，例如NPC位置等不可改的则像基本信息或实时状态tab一样不可编辑，例如NPC记忆档案等可编辑性，还有NPC对话窗口的设计可相仿主要故事对话的逻辑。
此次策划内容较多且互相有一定的dependancy，因此计划档案是非常必要的，请根据以上内容与我共同完成设计档案。你必须在写的过程中不断与我对话并问我问题，我们逐步完成。

六、NPC 专属小任务
核心定位：强化玩家与 NPC 的羁绊，同时解锁隐性特质
细化落地：
设计生活化任务类型：寻物（如找丢失的玉佩）、倾诉（如吐槽烦恼）、陪伴（如下棋 / 放风筝）
任务奖励以好感度提升、隐性特质解锁为主，无实质道具奖励
任务随机触发，触发概率与情感记忆日志中的好感度正相关
跨模块交互：
任务完成情况自动写入情感记忆日志，更新 NPC 对玩家的情感状态
任务解锁的隐性特质补充至多维人设体系，调整特质权重
任务场景会留下 NPC 世界互动痕迹（如陪伴下棋→留下棋盘痕迹、寻物→留下搜索痕迹）

七、NPC之间的关系更新
周期性更新NPC之间的关系，关系更新时需要更新NPC的记忆与情感记忆日志。
可以在世界心跳系统中更新。

API和prompt的关系：
第一个api内容：role: system 使用分步第1步的内容； role: assistant 使用记忆 role: user 使用玩家输入
第二个api内容：role: system 使用分步第2步的内容； role: user 使用玩家输入加上第一个api返回的正文



prompt删减计划：
--- 主回合部分 ---
# 地点势力不重叠（铁律）

# 冲突回合制（重要）

# 【数据结构定义】通用版 (ming) 精简存档

## 2. 背包与物品（路径：角色.背包）
- 灵石: {下品: number, 中品: number, 上品: number, 极品: number}（通用货币，用 add 增减） --改为金钱
### 2.2 物品通用字段 --需要修改
### 2.3 装备类型特有字段 --需要修改

# 品质系统 --需要修改

- 气血: {当前, 上限} (生命/体力，当前用 add，上限在成长时用 add) -- 改为体力
- 灵气: {当前, 上限} (精力/能量，当前用 add，上限在成长时用 add) -- 改为精力